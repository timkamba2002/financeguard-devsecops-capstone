replicaCount: 2

image:
  tag: "latest"

env:
  ENVIRONMENT: "dev"
  SKIP_AUTH_VERIFICATION: "true"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

ingress:
  enabled: true
