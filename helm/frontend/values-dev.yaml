replicaCount: 2

image:
  tag: "latest"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

ingress:
  enabled: true

env:
  # Actual dev backend ALB hostname — update if the ingress is recreated.
  # Run: kubectl get ingress -n dev -o jsonpath='{.items[0].status.loadBalancer.ingress[0].hostname}'
  BACKEND_API_URL: "http://k8s-dev-financeg-ff3bdf31f7-1789545035.us-east-1.elb.amazonaws.com"
